language: rust
rust:
  - nightly

os:
  - linux
  - osx
  - windows

before_script:
  - cargo install cargo-prune --force
script:
  - set -x;
    cargo test --verbose --release

cache:
  cargo: true
before_cache:
  - cargo prune
